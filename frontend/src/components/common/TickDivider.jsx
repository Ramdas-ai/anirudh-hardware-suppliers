// Signature structural device: a tick-mark "measuring tape" rule.
// Used between major sections site-wide instead of a plain line, tying the
// visual language back to the shop's own tools.
export default function TickDivider() {
  return <div className="tick-divider" role="presentation" />
}
